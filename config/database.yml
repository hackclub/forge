# PostgreSQL configuration
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: quarry_development

test:
  <<: *default
  database: quarry_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
